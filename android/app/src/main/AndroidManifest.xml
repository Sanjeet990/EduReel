<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".EduReelApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduReel"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.EduReel.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.onboarding.OnboardingActivity" />
        <activity android:name=".ui.feed.HomeActivity" />
        <activity android:name=".ui.feed.SavedFeedActivity" />
        <activity android:name=".ui.feed.WatchedFeedActivity" />
        <activity android:name=".ui.paywall.PaywallActivity" />
        <activity android:name=".ui.profile.SettingsActivity" />
        <activity android:name=".ui.common.BlockedActivity" />
        <activity android:name=".ui.feed.PublicProfileActivity" />
        <activity android:name=".ui.feed.CreatorFeedActivity" />
        <activity android:name=".ui.profile.FollowListActivity" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        
        <activity android:name=".ui.explore.ExploreFeedActivity" />
    </application>

</manifest>
